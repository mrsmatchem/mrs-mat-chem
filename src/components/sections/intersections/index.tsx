import React from "react";
import { IntersectionDataProps } from "../../../../lib/types";

const Intersection = ({ data }: { data: IntersectionDataProps }) => {
  return <div>Intersection</div>;
};

export default Intersection;
